#!/bin/bash

set -euo pipefail

# For buildkite-agent subcommands, execute directly without sbatch
if [[ "$BUILDKITE_COMMAND" == *"buildkite-agent "* ]]; then
    eval "$BUILDKITE_COMMAND"
    exit $?
fi

# Build sbatch arguments as a bash array to avoid quoting issues
sbatch_args=()
has_container_image=false
user_container_mounts=""
user_container_workdir=""

for var in $(compgen -e); do
    if [[ $var == slurm_* ]]; then
        # Strip the slurm_ prefix and convert to sbatch flag format
        opt_name=${var#slurm_}
        opt_name_lower=$(echo "$opt_name" | tr '[:upper:]' '[:lower:]')
        opt_name_hyphen=$(echo "$opt_name_lower" | tr '_' '-')

        # Get the value
        opt_value="${!var}"

        # Check if this is a container image
        if [[ "$opt_name_hyphen" == "container-image" ]]; then
            has_container_image=true
        fi

        # Handle container mounts and workdir specially to allow appending
        if [[ "$opt_name_hyphen" == "container-mounts" ]]; then
            user_container_mounts="$opt_value"
            continue
        fi

        if [[ "$opt_name_hyphen" == "container-workdir" ]]; then
            user_container_workdir="$opt_value"
            continue
        fi

        # Construct the argument
        if [[ "$opt_value" == "true" ]]; then
            sbatch_args+=("--$opt_name_hyphen")
        else
            sbatch_args+=("--$opt_name_hyphen=$opt_value")
        fi
    fi
done

# If container image is provided, add default mounts and workdir
if [[ "$has_container_image" == "true" ]]; then
    # Build container mounts: default workspace mount + any user-provided mounts
    container_mounts="${BUILDKITE_BUILD_CHECKOUT_PATH}:/workspace"
    if [[ -n "$user_container_mounts" ]]; then
        container_mounts="${container_mounts},${user_container_mounts}"
    fi
    sbatch_args+=("--container-mounts=$container_mounts")

    # Use user-provided workdir or default to /workspace
    container_workdir="${user_container_workdir:-/workspace}"
    sbatch_args+=("--container-workdir=$container_workdir")

    # Pyxis/enroot containers do not inherit all host env vars even with --export=ALL.
    # Explicitly pass all user-defined pipeline variables (non-slurm_, non-BUILDKITE_)
    # to the container via --container-env so they are available inside the job.
    container_env_vars=()
    for var in $(compgen -e); do
        if [[ $var != slurm_* ]] && [[ $var != BUILDKITE_* ]]; then
            container_env_vars+=("$var")
        fi
    done
    if [[ ${#container_env_vars[@]} -gt 0 ]]; then
        sbatch_args+=("--container-env=$(IFS=,; echo "${container_env_vars[*]}")")
    fi
fi

# Write BUILDKITE_COMMAND to a temporary batch script
tmpscript=$(mktemp /tmp/buildkite-sbatch-XXXXXX.sh)
trap 'rm -f "$tmpscript"' EXIT

printf '#!/bin/bash\nset -eo pipefail\n' > "$tmpscript"
printf '%s\n' "$BUILDKITE_COMMAND" >> "$tmpscript"

echo "--- :slurm: Submitting job with sbatch"
echo "{{ slurm_bin }}/sbatch --parsable --export=ALL ${sbatch_args[*]} $tmpscript"

job_id=$({{ slurm_bin }}/sbatch --parsable --export=ALL "${sbatch_args[@]}" "$tmpscript")
echo "Submitted batch job $job_id"

# Cancel the Slurm job if Buildkite cancels this step (SIGTERM) or the hook is interrupted (SIGINT)
trap '{{ slurm_bin }}/scancel "$job_id"' INT TERM

# Poll until the job leaves the queue (completed, failed, or cancelled)
while [[ -n "$({{ slurm_bin }}/squeue --job "$job_id" --format="%T" --noheader 2>/dev/null)" ]]; do
    sleep 5
done

# Retrieve the batch step exit code via sacct
exit_code=$({{ slurm_bin }}/sacct --job "${job_id}.batch" \
    --format=ExitCode --noheader --parsable2 2>/dev/null \
    | head -1 | cut -d: -f1)
exit "${exit_code:-0}"
